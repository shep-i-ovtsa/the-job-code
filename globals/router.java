public class router {
    String current_register ="valve";
    String ports = "/20";
    private String[] register ={"BLUE","RED","VAVLE"};
    private String[] register_type = {"valve","unkown","valve"};
    public void register(String ip, String pass, String where){
        if(pass == "temp" && ports == "temp"){
            //check password and if the ports the object is on, is the same as the ones scanned in the global ip table;
        }
    }
    public String request(String ip){
        return("packet");
    }
    public String send(String ip){
        return("packet");
    }
    //register
    //request
    //send
    //ip conf
    
}

